import PropTypes from 'prop-types'
import React from 'react'
import styled from '@emotion/styled'

const Footer = () => {
  return <footer>© {new Date().getFullYear()} - Bobby Lee</footer>
}

export default Footer
